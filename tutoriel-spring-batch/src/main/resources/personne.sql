-- script SQL pour la création de la table personne
CREATE TABLE PERSONNE (	ID INTEGER,
					    NOM VARCHAR (50),
					    PRENOM VARCHAR (50),
					    CIVILITE VARCHAR (3),
					    PRIMARY KEY (ID)
);